# Mise tool versions for matra.
#
# Nothing to pin here today. Python is managed by uv, which reads
# requires-python from pyproject.toml. Rust is managed by rustup, which reads
# rust-toolchain.toml. This file stays so direnv activation has a target and
# so a future tool has an obvious home.

[tools]
