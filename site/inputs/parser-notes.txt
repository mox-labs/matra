The parser reads each paragraph on its own. It splits the text into sentences and gives every word a lemma, a part of speech and a head.

A word's head is the word it depends on, and the label on that link names the relation. The model was trained on the English Web Treebank, so it is most reliable on edited English prose.
