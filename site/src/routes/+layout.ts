// Every route is prerendered: the site is static files on GitHub Pages.
export const prerender = true;
// `/guides/cli` is written as `guides/cli.html`, the path mdBook served.
export const trailingSlash = 'never';
