# The matra docsite

The documentation at <https://mox-labs.github.io/matra/>, built with SvelteKit
and prerendered to static HTML. The plan it follows, and the reasons for its
shape, are in [EP-0012](../blueprints/eps/0012-docsite.md). Its Design section
is the reference; this file is how to work on the site.

Until the cut-over (EP-0012, M2), the deployed site is still the mdBook build
of the same pages, from `book/`. This site is built beside it, checked by the
same gates, and uploaded by `docs.yml` as the `site-preview` artifact.

## Run it

You need [Bun](https://bun.sh). CI uses the version pinned as `BUN_VERSION`
in `.github/workflows/ci.yml`.

```bash
just docs-serve     # live preview at http://localhost:3000
just docs-build     # prerender everything into site/build
just docs-floor     # every docsite gate, both builds included
```

`just docs-serve` has no search: the Pagefind index is written from the built
HTML, so search answers only in a build. To browse a build locally, serve
`site/build` with any static file server. A build made with
`BASE_PATH=/matra` (as the GitHub Pages one is) must be served under `/matra`.

## Where things are

| Path | What it holds |
|---|---|
| `content/` | The pages, as plain Markdown. `SUMMARY.md` is the navigation. `roadmap.md` is a symlink to the repository's `ROADMAP.md`. `llms.txt` is generated by `scripts/gen-llms-txt.sh`. |
| `src/lib/server/content.ts` | Reads the pages and `SUMMARY.md` at build time. |
| `src/lib/server/summary.ts` | Parses `SUMMARY.md`. A line it cannot read fails the build. |
| `src/lib/server/markdown/render.ts` | Markdown to HTML, at build time. |
| `src/lib/server/markdown/registry.ts` | The tag registry. |
| `src/routes/` | One route per page, its `.md` twin, `llms.txt`, and the contents, print and 404 pages. |
| `src/app.css` | Tokens and page typography. |

## Pages are Markdown

A page is a Markdown file in `content/` and an entry in `content/SUMMARY.md`.
Nothing else: no front matter, no Svelte. The gates, the page-pinning tests in
`tests/`, `llms.txt` and any agent reading the repository all read the same
file, so the page source has to stay readable without a Svelte parser.

Each page is served twice: as HTML at `<path>.html`, and as its Markdown source
at `<path>.md`, for agents. `llms.txt` is served at the site root.

Links between pages are written as relative links to the `.md` file
(`../guides/cli.md#install`), which is how they work on GitHub too. The renderer
rewrites each to the route it is served at. A link that resolves to no page in
`SUMMARY.md` fails the build.

## The tag registry

Markdown produces a small, known set of HTML elements. Any other element in a
page was written as raw HTML, and it renders only if its tag is registered in
`src/lib/server/markdown/registry.ts`. An unregistered tag fails the build and
names the file, the line and the tag. That also catches a bare `<name>` in
prose that was meant as text: put it in backticks.

Today the registry holds one entry, `svg`, a passthrough for the seven
hand-authored diagrams: the element and everything inside it are emitted as
written. Figures generated from matra's output (EP-0012, M3) will add a second
kind of entry that maps a tag to a component.

A code fence in a language the highlighter has no grammar for also fails the
build. Add the language to `LANGUAGES` in `render.ts`.

## Design rules, in brief

The full reasoning is in EP-0012's Design section.

- **Rendered HTML is in the page.** Every page is prerendered, and its content
  does not depend on a script. The Markdown pipeline and the highlighter run at
  build time and never reach the browser. With scripts off, the pages read the
  same; search, the menu and the theme toggle hide themselves.
- **Typography first.** A measure near 80 characters, 17px body text, headings
  bound to the text they introduce, code blocks and tables that sit inside the
  rhythm. Diagrams and tables keep their width; a table wider than the column
  scrolls on its own. No page scrolls sideways at 360px.
- **Light and dark from one set of tokens.** Each colour is declared once with
  `light-dark()`. The site follows `prefers-color-scheme` until the reader picks
  a theme with the toggle, and remembers the choice.
- **No motion.** Nothing transitions or animates, and `prefers-reduced-motion`
  is respected globally. Figures that move on the reader's request arrive with
  EP-0012's later milestones, under its Motion rules.
- **URLs are a contract.** `/guides/cli` is written as `guides/cli.html`, the
  path mdBook served, and heading ids match mdBook's. Gate 8 of the docsite
  floor (`scripts/check-url-parity.sh`) fails on any path or heading anchor
  mdBook served that this site does not.

## Gates

`just docs-floor` runs `scripts/check-docsite-floor.sh`, which is also the
`Docsite floor` job in CI. For this site it runs `bun install
--frozen-lockfile`, `svelte-check` with warnings as failures, and the build
(gate 7); lychee over the built HTML, fragments included (gate 1); and URL
parity with the mdBook build (gate 8). The other gates read `content/`.

Dependencies are pinned exactly in `package.json` and locked in `bun.lock`;
Dependabot moves the pins. The Bun binary is pinned by version and SHA-256 in
the workflows.

## Comments

Readers signed in with GitHub can comment on a page, with each page's comments
stored as a GitHub Discussion in this repository, through
[giscus](https://giscus.app). The component is `src/lib/components/Giscus.svelte`.
It renders only when the build sets both of these variables, and is invisible
otherwise:

| Variable | Value |
|---|---|
| `PUBLIC_GISCUS_REPO_ID` | The repository's node id: `R_kgDORzS1lQ` |
| `PUBLIC_GISCUS_CATEGORY_ID` | The node id of the discussion category comments go into |

Each page maps to one discussion by its path (`pathname`, with `strict`
matching). giscus drops the extension before matching, so `/matra/guides/cli`
and `/matra/guides/cli.html` share a discussion. Nothing loads from giscus.app
until the comments are scrolled near, and the widget's theme follows the site's.

To switch comments on:

1. Install the giscus GitHub App on the repository
   (<https://github.com/apps/giscus>) and enable Discussions. Both were done
   for this repository in September 2026.
2. Create a discussion category for them, named `Docs`. The Announcement type
   is the one giscus recommends: only maintainers and giscus can open a
   discussion in it, so every thread starts from a page.
3. Read the category's id, from the configuration form on <https://giscus.app>
   or from the API:

   ```bash
   gh api graphql -f query='{ repository(owner: "mox-labs", name: "matra") {
     id discussionCategories(first: 20) { nodes { id name } } } }'
   ```

4. Set `PUBLIC_GISCUS_CATEGORY_ID` to it in the `Build the SvelteKit site
   preview` step of `.github/workflows/docs.yml`, beside the repository id
   that is already there. Comments then appear in the `site-preview` build,
   and on the live site once it deploys from here.
5. Optionally, commit a `giscus.json` at the repository root with
   `{ "origins": ["https://mox-labs.github.io"] }`, so the widget refuses to
   run on any other site.

To try it locally, set both variables when starting the dev server:
`PUBLIC_GISCUS_REPO_ID=... PUBLIC_GISCUS_CATEGORY_ID=... just docs-serve`.
